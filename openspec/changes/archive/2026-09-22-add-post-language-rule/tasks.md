# Tasks

## 1. Documentar la regla

- [x] 1.1 Archivar este cambio para que la capability `post-content` quede en `openspec/specs/post-content/spec.md` con la regla de español neutral; verificar con `openspec list --specs` que aparezca `post-content`
- [x] 1.2 Registrar la regla donde la vea quien redacta contenido (p. ej. una nota en `categories.md` o en el layout del admin), para que no viva solo en la spec; verificar leyendo el archivo actualizado

## 2. Aplicar a contenido existente

- [x] 2.1 Revisar los posts ya publicados y corregir voseo o regionalismos hacia español neutral; verificar releyendo cada post ajustado y confirmando el uso de "tú" sin modismos locales
