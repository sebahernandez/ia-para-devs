# Tasks

## 1. Materializar la spec

- [x] 1.1 Archivar este cambio para que la capability `deployment` quede en `openspec/specs/deployment/spec.md`; verificar con `openspec list --specs` que aparezca `deployment`

## 2. Documentar el proceso

- [x] 2.1 Dejar el proceso de release por escrito donde se ejecuta (p. ej. una sección "Deploy" en `CLAUDE.md` o un `DEPLOY.md`): orden GitHub → rebuild, precondición de aprobación, y el caso de contenido solo-en-Neon; verificar leyendo el archivo
